{
  "name": "your-action-app",
  "version": "0.0.1",
  "engines": {
    "node": "~6.0"
  },
  "scripts": {
    "lint": "semistandard --fix \"**/*.js\"",
    "test": "npm run t test/*.test.js",
    "deploy": "gcloud alpha functions deploy aura --source-url https://source.developers.google.com/p/project=personal-projects-1369/r/aura --source-path . --trigger-http --source-branch master --entry-point root"
  },
  "dependencies": {
    "actions-on-google": "^1.0.0"
  },
  "devDependencies": {
    "semistandard": "^9.1.0"
  }
}